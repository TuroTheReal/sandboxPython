"""Contains Duplicate.

Étant donné une liste d'entiers `nums`, renvoie True s'il existe au moins un
doublon (une valeur qui apparaît au moins deux fois), False si tous les
éléments sont distincts.

Exemples:
    contains_duplicate([1, 2, 3, 1]) -> True
    contains_duplicate([1, 2, 3, 4]) -> False
    contains_duplicate([])           -> False

Contraintes: la liste peut être vide, les valeurs peuvent être négatives.

Objectif: O(n) en temps. Applique le réflexe "ai-je déjà vu X ?".
Quand tu as fini, remplis la complexité en bas du fichier.
"""


def contains_duplicate(nums: list[int]) -> bool:
    raise NotImplementedError("À toi de coder")


# Time:  O(?)
# Space: O(?)
